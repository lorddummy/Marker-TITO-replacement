#!/bin/sh
MSG_FILE="$1"
[ -f "$MSG_FILE" ] || exit 0
grep -viE '^co-authored-by:.*cursor|^made-with:.*cursor|cursoragent@cursor' "$MSG_FILE" > "${MSG_FILE}.tmp" || true
mv "${MSG_FILE}.tmp" "$MSG_FILE"
